<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #2196F3; color: white; padding: 20px; border-radius: 5px 5px 0 0; }
    .content { background-color: #f9f9f9; padding: 20px; border-radius: 0 0 5px 5px; }
    .event-details { background-color: white; padding: 15px; margin: 15px 0; border-left: 4px solid #2196F3; }
    .client-info { background-color: white; padding: 15px; margin: 15px 0; border-left: 4px solid #FF9800; }
    .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #ddd; font-size: 12px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>New Event Scheduled 📅</h1>
    </div>
    <div class="content">
      <p>Hi <%= @user.name %>,</p>
      <p>A new event has been scheduled on your calendar!</p>

      <div class="event-details">
        <h3><%= @event.title %></h3>
        <% if @event.description.present? %>
          <p><%= @event.description %></p>
        <% end %>
        <p><strong>📅 Date:</strong> <%= @event.start_time.strftime('%A, %B %d, %Y') %></p>
        <p><strong>🕐 Time:</strong> <%= @event.start_time.strftime('%I:%M %p') %> - <%= @event.end_time.strftime('%I:%M %p') %></p>
      </div>

      <div class="client-info">
        <h4>Client Information</h4>
        <p><strong>Name:</strong> <%= @client.name %></p>
        <p><strong>Email:</strong> <%= @client.email %></p>
        <% if @client.phone.present? %>
          <p><strong>Phone:</strong> <%= @client.phone %></p>
        <% end %>
      </div>

      <p>The event has been added to the attached calendar file (<strong>event.ics</strong>). You can import this into your calendar application.</p>

      <p><em>A confirmation has been sent to <%= @client.email %><% if @client.phone.present? %> and <%= @client.phone %><% end %>.</em></p>

      <div class="footer">
        <p>This is an automated notification from Callab.</p>
      </div>
    </div>
  </div>
</body>
</html>
